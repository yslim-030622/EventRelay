spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/eventrelay
    username: eventrelay
    password: localpass
    hikari:
      maximum-pool-size: 10
  jpa:
    hibernate:
      ddl-auto: update
  data:
    redis:
      host: localhost
      port: 6379
  rabbitmq:
    host: localhost
    port: 5672
    username: guest
    password: guest

app:
  discord-webhook-url: ${DISCORD_WEBHOOK_URL:}
